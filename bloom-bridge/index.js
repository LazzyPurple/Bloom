const BRIDGE_HTTP_PORT = 9000;
const BRIDGE_WS_PORT = 8765;

console.info("[Bloom Bridge] Scaffold only. Phase 2 will implement localhost IPC and the LAN WebSocket server.");
console.info(`[Bloom Bridge] Reserved ports -> HTTP: ${BRIDGE_HTTP_PORT}, WS: ${BRIDGE_WS_PORT}`);
